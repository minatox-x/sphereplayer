// Top-level build.gradle — no buildscript block needed with new plugin DSL
allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
